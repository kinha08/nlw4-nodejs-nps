<style>
    .container {
        width: 800px;
        justify-content: center;
        align-items: center;
        align-content: center;
        display: flex;
        flex-direction: column;
    }

    .value {
        padding: 10px;
        background-color: #aa154a;
        color: white;
        border-radius: 6px;
        width: 15px;
        text-align: center;
        text-decoration: none;
    }

    .level {
        display: flex;
        margin: 10px;
        justify-content: space-between;
        width: 350px;
    }

    .answer {
        width: 350px;
        display: flex;
        justify-content: center;
        margin: 10px;
    }
</style>

<div class="container">
    <label>Olá <strong>{{name}}</strong>! Tudo bem com você?</label>

    <h3>{{title}}</h3>
    <strong>{{description}}</strong>

    <div class="level">
        <span>Pouco Provavel</span>
        <span>Muito Provavel</span>
    </div>

    <div class="answers">
        <a class="value" href="{{link}}/1?u={{user_id}}">1</a>
        <a class="value" href="{{link}}/2?u={{user_id}}">2</a>
        <a class="value" href="{{link}}/3?u={{user_id}}">3</a>
        <a class="value" href="{{link}}/4?u={{user_id}}">4</a>
        <a class="value" href="{{link}}/5?u={{user_id}}">5</a>
        <a class="value" href="{{link}}/6?u={{user_id}}">6</a>
        <a class="value" href="{{link}}/7?u={{user_id}}">7</a>
        <a class="value" href="{{link}}/8?u={{user_id}}">8</a>
        <a class="value" href="{{link}}/9?u={{user_id}}">9</a>
        <a class="value" href="{{link}}/10?u={{user_id}}">10</a>
    </div>

    <div>
        <h3><strong>Sua opnião é muito importante para nós!</strong></h3>
        <h3>Equipe | <strong>Nobre</strong></h3>
    </div>
</div>